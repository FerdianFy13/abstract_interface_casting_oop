class CodeIgniter {
  // ignore: unused_field
  String project;

  // method in interface
  String mvc() =>
      'CodeIgniter uses the Model, View, Controller (MVC) pattern to organize the files.';
}
